extends layout

block content
  .container.text-center.col-4
    p= contacts.Name
    p= contacts.Email_id
    p= contacts.Mobile_no
    hr
    a.btn.btn-default(href="/users/contacts/edit/" + contacts._id) EDIT
    a.btn.btn-danger.delete-contact(href="#", data-id = contacts._id) DELETE  
